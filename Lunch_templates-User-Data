#!/bin/bash -ex
exec > >(tee /var/log/user-data.log|logger -t user-data -s 2>/dev/console) 2>&1
cd 
#clonwoking repo
git clone -b master https://github.com/RainBowCreation/autoscale.git
#install java
cd autoscale
set JAVA_VERSION="17.0.9-amzn"
set PORT=25565
chmod +x ./setup.sh
sudo ./setup.sh
